# Secrets
.env

# Local build archives (not for version control)
_archive/

# Generated build and distribution artifacts
build/
dist/
*.egg-info/

# Runtime logs
logs/

# macOS
.DS_Store

# Python environments and caches
.venv/
__pycache__/
*.py[cod]
.mypy_cache/
.pytest_cache/
.ruff_cache/
.hypothesis/

# Local tooling artifacts
.coverage
.mplconfig/
.pyinstaller/
.pyinstaller_run2/

# Build and packaged outputs
build/
dist/
*.spec
*.zip

# Test/report artifacts
reports/
tests/__pycache__/
tools/__pycache__/

# Internal development artifacts (not for public repo)
bugs.txt
unit_tester.txt
APP_PORTING_HANDOFF.txt
content_jsons.txt
curriculum.txt

# Auto-generated at app startup — not source files
data/slide_capacity_report.json
data/slide_capacity_report.txt

# JS lint tooling — `package.json` declares the eslint devDeps and
# `package-lock.json` (committed) pins the full transitive graph for
# reproducible CI installs via `npm ci`. `node_modules/` and the eslint
# cache are regenerated on each install and never committed.
node_modules/
.eslintcache

# Local Claude Code / agent tooling (machine-specific, not for public repo)
.claude/
.helicops/
.mcp.json
AGENTS.md
CLAUDE.md
docs/ORCHESTRATOR_HANDOFF.md
notes/
