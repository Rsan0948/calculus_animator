# Calculus Animator — Hugging Face Space dependencies.
#
# This file is the Space-flavored dependency list (Gradio + matplotlib for
# the headless container; no pywebview/pygame). The desktop branch on GitHub
# uses a different requirements.txt with the desktop shell + render stack.
#
# Install (Space picks this up automatically; for local Space-mode dev):
#   pip install -r requirements.txt

sympy==1.14.0
numpy==2.4.4
antlr4-python3-runtime==4.11.1

# AI tutor backend deps (router is imported lazily in app.py)
fastapi==0.135.3
uvicorn==0.44.0
httpx==0.28.1
pydantic==2.12.5

# Gradio Space UI + headless plotting
gradio==4.44.0
matplotlib>=3.8
